import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.learntocode.trillo',
  appName: 'trillo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
